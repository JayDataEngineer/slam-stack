{
  "name": "slam-stack-browser-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright browser tests for slam-stack services",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.0.0"
  }
}
